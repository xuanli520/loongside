---
title: NVIDIA
description: Configure the NVIDIA provider profile in Loong.
---

# NVIDIA

Use this when your team already runs on NVIDIA and you want that vendor family to stay explicit in Loong config instead of disappearing behind a generic compatibility route.

## At A Glance

| Field | Value |
| --- | --- |
| Built-in kind | `nvidia` |
| Provider group | Direct Hosted Providers |
| Protocol family | `openai_chat_completions` |
| Feature family | `openai_compatible` |
| Auth scheme | `bearer` |
| Credential envs | NVIDIA_API_KEY |
| Aliases | `nvidia_compatible`, `nvidia_nim`, `nvidia-nim`, `build.nvidia.com` |
| Default base URL | `https://integrate.api.nvidia.com` |
| Request endpoint | `https://integrate.api.nvidia.com/v1/chat/completions` |
| Models endpoint | `https://integrate.api.nvidia.com/v1/models` |

## Minimal Config

```toml
active_provider = "nvidia"

[providers.nvidia]
kind = "nvidia"
api_key = { env = "NVIDIA_API_KEY" }
model = "auto"
```

## Verify It

```bash
loong doctor
loong list-models
loong ask --message "Say hello and name the active provider."
```

If `list-models` is unreliable for this account or region, pin an explicit `provider.model` or add `preferred_models` instead of leaving recovery implicit.

## Auth And Routing Contract

| Contract | Value |
| --- | --- |
| Auth optional | no |
| Model probe auth optional | no |
| Default API key env | `NVIDIA_API_KEY` |
| OAuth env | none |
| Primary request route | `https://integrate.api.nvidia.com/v1/chat/completions` |
| Primary model-catalog route | `https://integrate.api.nvidia.com/v1/models` |

## Related Docs

- Continue to [Providers And Models](/use-loong/providers-and-models) when you still need the broader chooser page.
- Continue to [Provider Guides](/use-loong/provider-guides/index) for the full built-in provider matrix.
- Continue to [Provider Recipes](/use-loong/provider-recipes) for representative rollout recipes.
- Continue to [Configuration Patterns](/use-loong/configuration-patterns) for the shared provider-profile shape.
